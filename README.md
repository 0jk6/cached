# cached

simple key, value store

### how to run?
```
make run port=3000

or

make build

./main port-number

eg: ./main 3000


then use nc or telnet to connect to the server

supported commands

SET key value
GET key
DEL key
```